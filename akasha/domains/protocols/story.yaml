type: domain
id: domain-protocols
level: L1
who: AI agent
why: |
  pneuma-core の "外側" との接続境界を一括で定義する Protocol 群。
  外部 I/O (LLM / Storage / Memory / Embedding / Voice / Task /
  Middleware) はすべてここで型として宣言され、実装は別ドメイン
  または利用側に委ねられる。
modules:
  - llm
  - storage
  - memory_store
  - embedding
  - middleware
  - voice
  - task
