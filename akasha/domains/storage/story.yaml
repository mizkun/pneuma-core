type: domain
id: domain-storage
level: L1
who: AI agent
why: |
  キャラクターの内部状態 (Character / Memory / EmotionalState / Goals /
  Relation / ChangeLog / Todo) を永続化する層。StorageBackend Protocol で
  抽象化されており、SQLite / InMemory が同梱、それ以外は外部実装に委ねる。
modules:
  - backend
  - sqlite
  - in_memory
