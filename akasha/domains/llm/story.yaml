type: domain
id: domain-llm
level: L1
who: AI agent
why: |
  LLM プロバイダおよび Embedding サービスとの境界を成す層。
  Protocol で抽象化された LLMAdapter / EmbeddingService の実装を提供する。
  anthropic / openai への直接依存を許容するのはこの層のみ。
modules:
  - adapter
  - claude
  - embedding
