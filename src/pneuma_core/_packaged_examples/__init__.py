"""Packaged example character sheets bundled with pneuma_core.

The proper place for user-facing examples is the repo-root `examples/` dir.
This subdirectory exists because the Coding Agent role can only write under
`src/*` — the contents here will be moved to `apps/aituber/` or `examples/`
in a follow-up Issue.
"""
