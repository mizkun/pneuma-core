# Pneuma Plan — ローンチまでのロードマップ

Vision（`vision.md`）とコア・コンセプト「神の実験室」（`.vibe/references/concept-experiment-theater.md`）を実現するための計画。

> **2026-05-29 改訂**: Codex + Agent Teams 5 視点の徹底レビュー（`.vibe/references/roadmap-review-2026-05-29.md`、判定 **GO-with-fixes**）を反映。事実誤記の訂正、面白さ検証ゲートの前倒し、段階2の縮退ライン、録画ローンチ化、ローンチゲートの明確化を行った。

**ローンチの定義**: 最初の「実験記録」を世に出す = 1 つの実験（ゴール + ルール + 介入 + 結果）を実施し、**note 記事（主）+ YouTube アーカイブ（従）** として公開すること。**初回は生ライブにしない**（BAN リスク回避、下記 F）。

開発は AI 主導。人間の工数感ではなく「壁時計時間」「コスト」「ユーザー判断密度」で律速される。

---

## 全体像

```
Phase 0（完了）   コア基盤: N体会話 + 思惑/トグル + 計器の信頼性 + ローカル観察UI
   ↓
Phase 1（M1）    神の実験室 MVP → ローンチ
   ├─ 段階2 状態シリアライズ（MVP最大の技術リスク）
   ├─ 段階3 途中介入
   ├─ ★チープ検証ゲート（面白さ仮説の検証 / 撤退ライン）← ここで投資継続を判断
   ├─ 段階5 達成判定（軽ゴール・機械判定）
   ├─ 段階6 実験記録 + 録画公開パイプライン
   ├─ ローンチゲート（#10 コスト上限 / #11 Moderation縮小 / オリジナルキャラ確定 / 月hardcap）
   └─ 最初の実験を実施 → note + YouTube アーカイブで公開
   ↓
Phase 1.5      分岐/DAG（段階4）/ 長期記憶RAG配線 / 生ライブ化 / 世界線ツリーUI
   ↓
Phase 2+        物語化 / 24-7化+会社 / ビジュアル成熟 / VR / OSS・論文
```

---

## Phase 0 — コア基盤（✅ 完了）

| 項目 | Issue | 状態 |
|---|---|---|
| N 体会話基盤（multi_agent） | #6 | ✅ |
| emotion_label 単一定義 | #9 | ✅ |
| テキスト試聴ゲート（CLI + ローカル web UI） | #7 | ✅ |
| LLM 構造化出力保証（Tool Use + Validation + Retry） | #12 | ✅ |
| 思惑エンジン（実験の操作変数の一つ） | #20 | ✅ |
| 面白さエンジン（トグル: クセ/テンポ/水平思考/感情/オチ） | #22 | ✅ |
| 計器（観測系）の信頼性バグ修正 | #23 | ✅ |

**到達点**: ゆるキャン野クル組 3 体（※暫定・版権キャラ。公開前にオリジナル化必須、下記 E）がリアル Claude で自律会話し、内部状態が観察 UI で見える。

**⚠️ Phase 0 では完了していない（旧 plan の誤記を訂正）**:
- #10（コスト累計上限）= **OPEN**。実装済みの CircuitBreaker は **session 単位のみ**で、巻き戻し/分岐による多 session 高速生成のコスト暴走を防げない。
- #11（Moderation / kill switch）= **OPEN**。コード上に moderation / kill-switch は存在しない。
- → 両方とも**ローンチゲート**（Phase 1 完了条件、下記）。

---

## Phase 1 — 神の実験室 MVP（M1）→ ローンチ

実装は `apps/aituber/`（現状空ディレクトリ）+ pneuma-core 拡張。**初回ローンチはローカル運用 + 録画**で十分（Firebase 本番化は Phase 1.5 以降）。

### 実装ロードマップ（段階）

| 段階 | 内容 | 状態 | 規模感（レビュー反映） |
|---|---|---|---|
| 1 | 計器の信頼性（観測値とテキストの整合） | ✅ #23 | — |
| 2 | **状態シリアライズ + 復元** | 未 | **大（MVP最大の技術リスク・山場）** |
| 3 | **途中介入**（進行中に介入を差し込む） | 未 | 中（intervention.yaml で意味論を先に定義） |
| ★ | **チープ検証ゲート**（下記）| 未 | — |
| 5 | **達成判定（軽ゴール・機械判定）** | 未 | 中（看板実験は外す） |
| 6 | **実験記録の構造化 + 録画公開パイプライン** | 未 | 中（計器は既存 web_server 拡張） |

**段階2 の設計指針（レビュー D 反映）**:
- 現 `snapshot()` は lossy な観測ダンプ（round 丸め・切り詰め）。**そのまま復元基盤にできない**。
- `to_state()` / `from_state()`（無損失・round なし）を **snapshot() と別物として新規分離**。restore は新 session 再構築の classmethod（in-place は frozen Character / LLM 参照で事故る）。
- `from_state(to_state(s)) はターン進行が等価` を **Red テストで先に縛る**。
- **縮退ライン**: 詰まったら分岐 DAG（段階4）を Phase 1.5 に後回しし、**「snapshot + 単純巻き戻しのみ」でローンチ**する。

**段階4（分岐 + 履歴 DAG）は Phase 1.5 に降格検討**: 統制実験は LLM 非決定性で成立しない（下記 B）ため、初回は「snapshot + 単純巻き戻し」で十分。世界線ツリー UI も新規フロント工数なので Phase 1.5。

### ★ チープ検証ゲート（段階3直後・最重要）

レビュー A 反映。**面白さ仮説の検証を、作り込み（段階4-6）の前に置く。**

- 同一起点から「介入あり / 介入なし」の 2 分岐を回し、PO が 3 点チェック:
  1. PO が最後まで観られたか
  2. 計器（PAD/関係性/episodic）に観測可能な差分が出たか
  3. 台本でない創発の瞬間が最低 1 つあったか
- restore 完成を待たず、**session 再起動による手動巻き戻しでよい**。
- **撤退ライン（kill criteria）**: 3 セッション連続で 3 点に届かなければ、段階4-6 投資を止め、**面白さ仮説（介入で観るに足る創発が起きる）の見直しに戻る**。

### ローンチゲート（Phase 1 完了条件・ブロッカー）

ローンチ前に**必須**:
- [ ] #10: コスト累計上限（アプリ側カウンタ）+ **Anthropic Console の月 hard cap 物理設定**（PO のアカウント操作、コードより先）
- [ ] #11: Moderation 縮小版（録画の人手レビュー + 未成年題材の構造ガード + kill switch スクリプト）
- [ ] **オリジナルキャラ確定**（下記 E、PO 個人タスク）
- [ ] 1 実験を回して note 記事 + YouTube アーカイブを出せる

### 公開アクション（ローンチ・録画ベース）

| 出口 | 形式 | 主訴求 |
|---|---|---|
| **note（主）** | 実験記録・観察日誌（仮説→介入→状態 diff→反応→判定） | 創発の面白さ + アルゴリズム実証 |
| **YouTube（従）** | 録画アーカイブ（5-8 分の編集済み実験記録、OBS 画面録画） | 神の介入で何が起きたか |
| X | 切り抜き・告知（手動、PO 個人タスク） | 拡散 |

生ライブ化は数本回した後の **Phase 1.5**。

### 触る構造化 spec（概要）

- **新規**: `stories/intervention.yaml`（介入の型・ルール制約。実装前に invariant 化）
- **新規**: `stories/timeline.yaml`（to_state/from_state・round-trip 等価・巻き戻し。DAG は Phase1.5）
- **新規**: `stories/experiment.yaml`（ゴール・ルール・達成判定・実験記録。invariant: 同一介入は複数試行で傾向評価 / 達成判定は根拠ターンを伴う）
- **拡張**: `stories/multi-agent.yaml`（途中介入対応 + 感情推定の間引き）

---

## Phase 1.5 — ローンチ直後の強化

- 段階4: 分岐 + 履歴 DAG（content-addressable JSON 木で永続化）
- 長期記憶 RAG 配線（レビュー H）: SessionEnd→storage 永続化→次セッション RAG recall→prompt 注入。`session-end-state-persisted` invariant の spec-code drift 解消。差別化軸2「記憶バイアス RAG」の根拠を会話ループに通す
- 生ライブ化（Moderation を自動配信用に拡張）
- 世界線ツリー UI（新規フロント）
- Firebase + Cloud Run 本番化（ADR-0001）

---

## Phase 2+ — 中長期

### M2. 物語化レイヤー + 外部発信
実験記録の切り抜き・note 連載・動画化、キャラ SNS 能動発信

### M3. 24/7 化 + Phase 2 世界（会社・5〜10 体）
真の連続稼働（prompt cache + Haiku + 活動リズム）、会社シーン・社会関係

### M4. ビジュアル成熟 / M5. VR 同居 / (optional) Mx. OSS + arXiv
Live2D→3D→音声 / VR / pip 公開 + 評価実験（介入実験データが論文素材に）

---

## コスト感（レビュー反映・試行錯誤込み）

1 ターン = 発話1 + 全キャラ感情推定 N + intent 生成（初回）+ 整合補正。**巻き戻し・分岐で複数世界線を回すと指数的に増える**。

- 旧 plan の「月数千円〜1万円」は単一通し前提。試行錯誤込みでは上振れする。
- 削減策: 感情推定を毎ターン全キャラ→**発話者+関係者のみに間引き**、prompt cache 併用、Haiku 使い分け。
- **物理ガード**: Anthropic Console の月 hard cap（コードより先・最優先）。

---

## リスク（レビュー反映）

| リスク | 緩和 |
|---|---|
| **面白さ未達**（観察対象がまだエンタメになってない） | チープ検証ゲートを段階3直後に前倒し + 撤退ライン。**最大リスク** |
| **段階2 復元の複雑さ**（MVP 最大の技術リスク） | to_state/from_state 分離 + round-trip Red テスト + 縮退ライン |
| **BAN（未成年キャラ×AI×配信）** | **録画ローンチ化** + 人手レビュー + 構造ガード + kill switch。生ライブは後 |
| **IP/法務（版権キャラ）** | オリジナルキャラ確定をローンチゲートに |
| **コスト暴走（試行錯誤で多 session）** | #10 累計上限 + 月 hard cap 物理設定（session 単位 CircuitBreaker だけでは不足） |
| 達成判定のブレ | 軽ゴール機械判定 + 根拠ターン必須。看板はクリティカルパス外 |
| 統制実験が成立しない（LLM 非決定性） | 「反復観察・複数試行」に正直修正（下記 B） |
| PO 判断の律速 | 簡素でも可。キャラはオリジナル最小要件で |

---

## 戦略原則

1. **観察対象であること自体がエンタメ** — 本物の創発 + 介入の謎で勝負（ただし未実証 → チープ検証ゲートで検証）
2. **台本化しない = 非結末固定** — 結末・特定セリフを決めず、初期条件・環境刺激・制約・観測指標（入力）を設計する。NG = 結末指定/セリフ指定/成功するまで都合よく編集
3. **計器の信頼性が前提** — 観測値とテキストの整合（✅ #23）
4. **統制実験ではなく反復観察** — LLM 非決定性のため決定論を約束しない。同一介入は複数試行で傾向評価
5. **実験記録がコンテンツ単位** — 「1 実験 = ゴール+ルール+介入+結果」が note / 切り抜きの単位
6. **両軸を 1 つで** — 実験記録はエンタメであり、同時にアルゴリズムの実証
7. **完璧を待たずローンチ** — ローカル + 録画 + 簡素なビジュアルでも、最初の実験記録を出す
8. **検証を投資の前に** — 作り込んでから面白さ未達が判明する罠を、チープ検証ゲートで回避

---

## 未決定の論点（PO 判断待ち）

1. **月 hard cap の物理設定** — PO のアカウント操作（Iris 不可）。**最優先**
2. **キャラのオリジナル確定** — 名前/出自/Big Five/**Values 対立軸（self_transcendence vs self_enhancement 最低1本）**。ローンチゲート、PO 個人タスク
3. **最初の実験テーマ** — レビュー推奨は「特定話題への自発到達」（判定容易・diff 見せやすい）。「喧嘩→仲直り」も候補。看板（AI に AI と気づかせる）は第2-3号
4. **配信モード** — 録画 + note 主を推奨（レビュー F）。生ライブは Phase 1.5
5. **音声（TTS）** — 神の実験室では内部状態の変化が主役。録画ローンチなら音声は後でも可（Vision で MUST から降格検討）
6. **段階2 の最小復元集合** — Iris が設計、PO が方向性レビュー → 実装着手 GO
