{
  "name": "pneuma-core-akasha",
  "version": "0.0.0",
  "private": true,
  "description": "Akasha harness for pneuma-core. The runtime is Python (see pyproject.toml). This package only carries the Akasha config, Story YAMLs and Contract type definitions.",
  "type": "module",
  "main": "akasha.config.mjs",
  "scripts": {
    "test": "echo 'Python tests live in tests/. Run: .venv/bin/python -m pytest tests/ -x --tb=short' && exit 0"
  },
  "akasha": {
    "config": "./akasha.config.mjs",
    "product": "./akasha/product/story.yaml",
    "domains": "./akasha/domains",
    "contracts": "./akasha/contracts"
  }
}
